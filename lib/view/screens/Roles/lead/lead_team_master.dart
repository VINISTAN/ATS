import 'package:flutter/material.dart';

class LeadTeamMaster extends StatefulWidget {
  const LeadTeamMaster({Key? key}) : super(key: key);

  @override
  State<LeadTeamMaster> createState() => _LeadTeamMasterState();
}

class _LeadTeamMasterState extends State<LeadTeamMaster> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Lead Team Master")),

    );
  }
}
